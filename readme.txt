使用锁加条件变量控件第一个线程测试服务器的可用性，如果可用再通知启动其他线程。

加入retriever的虚拟机制。提高程序的抽象级别。

加入window的虚拟机制，以便能在以后方便的加入到窗口程序中，使之使用更方便。同时提高程序可扩展性。
提高程序的模块化程度。

加入log模块，提供调试功能及程序运行状态记录功能。


2.26
	修改退出不正常的错误。信号处理与线程同步部分。


下面的时候窗口标题不改变。加上这个功能。

处理http重定向的问题。
